#include <stdio.h>
#include <stdint.h>
#include "cursor.h"
#include "fps.h"

int main () {
    int a = 0;
    while(1) {
        // render_2d_array();
        render_screen();
    }
    
    
    getch();
}